# Plan de portage — Livret d'apprentissage → Next.js 16 / o2switch (étape 2)

Livrable annoncé au §1 de [`chantier-deploiement-o2switch.md`](chantier-deploiement-o2switch.md)
(arbitrage du 2026-07-14 : portage Next.js 16 App Router + Prisma 6 + MariaDB
+ Auth.js v5). Rédigé le 2026-07-14 sur inventaire réel du code (57 modules
`src/lib/`, 12 stores, 55 types exportés, 31 specs E2E).

---

## 0. Principes directeurs

1. **Parité fonctionnelle stricte.** Le fonctionnel est gelé : le portage ne
   change ni écran, ni règle (R1-R24), ni URL. La seule nouveauté visible est
   l'authentification réelle. La demande 2 (entretiens individuels) sera la
   première vague fonctionnelle POST-bascule.
2. **Les 220 E2E sont le contrat.** Chaque vague de portage se termine quand
   les specs Playwright correspondantes repassent sur la nouvelle app
   (sélecteurs `data-testid` reconduits ; seuls les helpers d'authentification
   changent — cf. §5).
3. **Les libs pures ne se réécrivent pas.** `src/lib/` (57 modules, ~767
   tests Vitest) est framework-agnostique : copie telle quelle. C'est l'actif
   principal du projet.
4. **Les droits s'appliquent CÔTÉ SERVEUR.** La matrice `droits.ts`
   (57 ressources × 6 rôles) devient le garde de chaque Server Action
   (`requireDroit(session, ressource)`) ; le client ne fait plus qu'afficher.
5. **Parité o2switch dès le premier jour** (doctrine §2.5) : MariaDB (jamais
   Postgres), `output: 'standalone'`, headers via Next, Nuage pour les
   binaires, mêmes clés d'env qu'en prod.

---

## 1. Inventaire de réutilisation

### A. Copié tel quel

| Quoi | Volume | Note |
|---|---|---|
| `src/types/index.ts` | 55 types | Reste la lingua franca applicative ; sert aussi de source de dérivation du schéma Prisma |
| `src/lib/` + tests | 57 modules, 54 fichiers de tests | Y compris parsers CSV/XLSX maison, trame d'entretien, matrice de droits, règles R1-R24 |
| `src/fixtures/` | 8 livrets, 2 promos, utilisateurs, référentiels, documents | Devient `prisma/seed.ts` (mêmes données — les E2E comptent dessus) |

### B. Adapté légèrement

| Quoi | Adaptation |
|---|---|
| `src/components/` (8 dossiers) | Composants clients (`"use client"`), props inchangées ; les lectures de stores Zustand remplacées par des props/hooks de données |
| `src/pages/` → `app/` | Mêmes URLs (App.tsx fait foi pour la table de routage) ; les pages deviennent des Server Components qui chargent les données et passent aux composants clients |
| `src/components/pdf/` | Reste 100 % client, chargé en `next/dynamic` `ssr: false` (équivalent du lazy actuel) — wrapper `Text` anti-cadratins conservé |
| Styles / tokens de rôle | Migration Tailwind 3 → 4 (config CSS-first) — les utilities `couleur-role` et la variable `--ring` dynamique à re-vérifier visuellement |
| `e2e/` (31 specs) | Reconduites vague par vague ; 2 adaptations transverses seulement (auth + seed, cf. §5) |

### C. Réécrit

| Quoi | Devient |
|---|---|
| 12 stores Zustand persistés | 3 couches : schéma **Prisma** (§2) + **Server Actions** par domaine (mutations gardées par droits + libs pures) + lecture par Server Components |
| RoleSwitcher / sélecteurs « actif » (maître, formateur, responsable) | Sessions réelles Auth.js v5 : Entra (staff) + credentials (externes). Le « rôle actif » devient LE compte connecté |
| Bump `VERSION_SCHEMA` / reset fixtures | Migrations Prisma versionnées + seed |
| `scripts/deploy.sh` / `verifier-vps.sh` | Pipeline du kit (`build:deploy` + runbook §0-9) + préflight o2switch à écrire |

### D. Non porté

- `ModaleFichePeriode.tsx` + mutations orphelines `useLivretStore` (dette
  connue, PROJECT-STATUS §8.B) — le portage la solde gratuitement.
- Bandeau démo / `BoutonReinitialiserDemo` (un mode démo serveur pourra
  exister via variable d'env, à trancher en V0).
- `persist` localStorage et tout le mécanisme de migration par reset.

---

## 2. Schéma de données — les 12 stores deviennent ~22 tables

**Principe d'arbitrage relationnel vs JSON** : relationnel pour tout ce qui se
filtre, se compte ou se joint (utilisateurs, formations, fiches, documents,
compétences…) ; colonne JSON pour ce qui se lit toujours d'un bloc et ne se
requête jamais transversalement (réponses de la trame d'entretien, objets
signature, historiques R10). Les **signatures PNG** (~10-20 Ko) restent en
base (TEXT) ; les **documents administratifs** (≤ 2 Mo) partent sur **Nuage**
(WebDAV), la base ne garde que la référence (doctrine §3.4).

| Store actuel | Tables Prisma | Notes |
|---|---|---|
| `livret-utilisateurs` v7 | `users` + `apprenti_responsables` (N:M) | Table unique, enum `role` (6 valeurs), champs spécifiques nullables ; 2 FK maître (principal/second), FK formateur/coordo/entreprise/formation ; `entra_oid` unique nullable (staff SSO) ; rattachement fratrie = N:M responsables |
| `livret-formations` v9 | `formations`, `periodes_formation` | Périodes entreprise + centre distinguées par un champ `lieu` ; mode d'évaluation + FK modèle d'activités |
| `livret-referentiels` v4 | `referentiels`, `blocs`, `competences` | `sousFamille`, `exclue` (limite lignes évaluables) portés tels quels |
| `livret-activites` v1 | `modeles_activites`, `activites`, `activite_competences` (N:M) | Le mapping activité ↔ compétences devient une table de jointure |
| `livret-attitudes` v3 | `attitudes` | Catalogue global (12) |
| `livret-parametres` v1 | `parametres` | Table clé-valeur (seuil lignes évaluables) |
| `livret-etablissements` v2 | `etablissements` | + URL Pronote |
| `livret-entreprises` v2 | `entreprises`, `affectations_entreprise` | Historique de traçabilité conservé en table (requêté par apprenti·e) |
| `livret-donnees` v26 | `livrets`, `fiches_suivi`, `lignes_suivi`, `entretiens`, `selections_entreprise` | Fiches entreprise/centre en une table (`lieu`) ; réponses de trame, commentaires, signatures, éval. attitudes par période : **JSON** ; sélection compétences/activités : table + historique R10 en JSON ; clôture / déverrouillages / points d'alerte traités / affichage forcé : colonnes JSON du livret |
| `livret-documents` v4 | `documents_administratifs`, `documents_formation`, `attestations` | Fichier = référence Nuage (`cheminNuage` remplace `dataUrl`) ; attestations indexées par apprenti·e (table — requêtées pour compteurs x/y et alertes) |
| `livret-role-actif`, `livret-apprenti-actif` | — (aucune table) | Remplacés par la session Auth.js et par l'URL / la sélection en page |
| *(nouveau — chantier 2.2/2.3)* | `activation_tokens`, `password_reset_tokens`, `login_attempts`, `audit_log` | Spécifiés dans `chantier-creation-comptes.md` §6 — `audit_log` couvre aussi l'obligation RGPD de journalisation |

Le schéma détaillé (types de colonnes, index, contraintes) est le premier
livrable de la vague V1 — dérivé mécaniquement de `src/types/index.ts`.

---

## 3. Architecture applicative cible

- **`app/`** : mêmes URLs qu'aujourd'hui (`/`, `/livret/…`, `/admin/…`) +
  routes d'auth (`/login`, `/activer/[token]`, `/mot-de-passe-oublie`).
  Layout = portage d'`AppShell` + `Sidebar` (la classe `role-actif-X` dérive
  de la session).
- **Server Actions par domaine**, calquées sur les stores actuels (un fichier
  d'actions par domaine : utilisateurs, formations, livret, entretien,
  documents…). Squelette systématique :
  `session → requireDroit(ressource) → lib pure (validation/règle) → Prisma → revalidate`.
  Les gardes croisées des stores (cascades, verrous de suppression) migrent
  dans ces actions — les libs `*-verrou.ts` existent déjà et se réutilisent.
- **`proxy.ts`** (middleware Next 16) : protection des routes par session +
  redirection `/login` ; headers de sécurité via `next.config.ts` (snippet A
  du kit).
- **Écran « compte en attente d'affectation »** pour un utilisateur tenant
  authentifié sans App Role ni compte applicatif (écart E4 de la fiche Entra).
- **Uploads** : Server Action → validation (lib `documents-administratifs`)
  → `putDocument()` WebDAV Nuage (doctrine §10.8) → référence en base.
  `bodySizeLimit` à 4 Mo (marge sur la limite métier de 2 Mo).

---

## 4. Ordre des vagues de portage

Chaque vague se termine par : tests Vitest verts (libs copiées) + specs E2E
de la vague vertes + `pnpm build:deploy` qui passe. Estimations grossières,
à recaler après V0-V1.

| Vague | Contenu | Specs E2E reprises | Estimation |
|---|---|---|---|
| **V0 — Bootstrap** | Kit déroulé (fichiers, snippets, marqueurs §3 du chantier), MariaDB Docker dev, copie types + libs + tests Vitest, page login squelette | — | ~0,5 j |
| **V1 — Socle données + auth** | Schéma Prisma complet + migrations + seed (fixtures actuelles), Auth.js credentials (comptes seedés) + provider Entra conditionnel, JIT + écran « en attente », `requireDroit`, AppShell/Sidebar | `sprint1-role-switcher` (adapté login), navigation | ~2-3 j |
| **V2 — Administration** | Pages admin : utilisateurs (+staff), formations (+planning), affectations, référentiels (+limite), activités (+mapping/mode), établissements, entreprises, attitudes, paramètres, import XLSX (+responsables légaux à l'import) | `admin-*`, `etablissements`, `entreprises`, `import-utilisateurs`, `limite-referentiel`, `responsables-legaux` (partie admin) | ~2-3 j |
| **V3 — Livret pédagogique** | Tableau de bord (+pilotage, alertes, récap apprenti·e), fiches de période entreprise/centre, organisation du suivi, entretien tripartite complet (trame, sélections, signatures tactiles, points d'alerte), synthèse, clôture R22 / déverrouillage R10 | `sprint2-5`, `fiches-periodes*`, `organisation-suivi`, `entretien-*`, `attitudes`, `signature-tactile`, `pilotage-alertes`, `tableau-de-bord-*`, `header-trio` | ~3-4 j |
| **V4 — Documents + Nuage** | Documents nominatifs + formation, attestations après lecture, attestataire responsable légal, stockage WebDAV Nuage | `documents-administratifs`, `responsables-legaux` (partie documents) | ~1-2 j |
| **V5 — PDF + mobile** | Export PDF lazy (7 sections + documents), page Accès mobile (QR), audit responsive | `export-pdf`, `acces-mobile`, `audit-mobile` | ~1-2 j |
| **V6 — Comptes externes + emails** | Chantiers 2.2/2.3 : activation par email, mot de passe oublié, rate limiting, `audit_log` (spec `chantier-creation-comptes.md` — SMTP à ré-arbitrer, lot E) | nouvelles specs (activation, reset) | ~2-3 j |
| **V7 — Déploiement + recette** | Runbook kit §0-9 sur `livret.gretacfalyon.com`, préflight o2switch (successeur de `verifier-vps.sh`), recette pilote, bascule | rejeu complet | ~1 j |

**Total : ~13-18 jours de développement.** Le VPS actuel reste en ligne
(démo) pendant toute la durée ; bascule DNS en V7 seulement.

---

## 5. Stratégie de reprise des E2E

Les 31 specs sont le filet de non-régression. Deux changements transverses,
tout le reste est reconduit :

1. **Authentification** : `e2e/helpers.ts` remplace le clic RoleSwitcher
   (aria-label) par une connexion réelle — `seConnecter(page, 'coordo')`
   se loge via les comptes seedés (credentials en test ; le SSO Entra se
   teste à la main en recette, pas en CI). Le « responsable actif » et le
   « formateur actif » deviennent des connexions de comptes distincts.
2. **Données** : le reset localStorage par spec devient un **reset de la base
   de test par spec** (base MariaDB dédiée + `prisma migrate reset --skip-seed`
   + seed rapide, ou endpoint de reset activé par variable d'env de test).
   Le piège « ne jamais lancer 2 suites Playwright en parallèle » demeure
   (base partagée).

Les compteurs de fixtures (8 apprenti·e·s, périmètres, Minh mineur…) restent
exacts puisque le seed reprend les fixtures actuelles.

---

## 6. Micro-décisions à trancher en V0

| # | Question | Recommandation |
|---|---|---|
| 1 | Dépôt : nouveau repo vs monorepo | **Nouveau dépôt privé** (ex. `livret-glm-app`) bootstrappé par le kit ; l'actuel `livret-glm` reste la maquette de référence jusqu'au décommissionnement |
| 2 | Tailwind 3 conservé vs 4 | **v4** (stack du kit) — migrer les tokens de rôles en config CSS-first, vérification visuelle dédiée |
| 3 | Zustand côté client ? | **Non** — état serveur via Server Components ; état UI éphémère en React local. Aucun store global |
| 4 | Signatures PNG | **En base (TEXT)** — petites, lues avec leur fiche ; seuls les documents vont sur Nuage |
| 5 | Mode démo post-bascule | Variable d'env (seed de démo + bannière) — utile pour les présentations sans données réelles, à confirmer |

---

## 7. Risques identifiés

- **Migration Tailwind 4** : la mécanique de tokens par rôle (`--ring`
  dynamique, ~50 utilities colorisées) est le point le plus « visuel » du
  portage → vérification visuelle systématique en V1 (doctrine du rituel).
- **react-pdf sous Next** : pattern connu (`next/dynamic`, `ssr: false`),
  mais à valider dès V0 sur un PDF miniature (le chunk de 494 Ko doit rester
  lazy).
- **Réécriture des helpers E2E** : c'est LE coût caché du portage — à faire
  proprement en V1 (tout le reste en dépend).
- **`bodySizeLimit` / uploads Passenger** : valider en V7 qu'un PDF de 2 Mo
  passe de bout en bout (Server Action → Nuage) sur o2switch.

---

## 8. Journal

| Date | Événement |
|---|---|
| 2026-07-14 | Plan rédigé (inventaire réel : 57 libs, 12 stores, 55 types, 31 specs). En attente du GO pilote pour V0. |
| 2026-07-14 | **V0 LIVRÉE** (GO pilote, micro-décisions §6 validées telles quelles) : dépôt local `../livret-glm-app` (commit `6f3cd34`) — Next 16.2.10 / React 19 / Tailwind 4 / shadcn, kit intégralement appliqué, Prisma 6 (binaryTargets CloudLinux, artefact v7 supprimé), **types + 57 libs + fixtures portés : 767/767 tests Vitest verts** (+ fflate), login squelette, MariaDB dev prêt (Docker éteint ce jour). **Risque react-pdf LEVÉ** (blob généré en navigateur sur le build standalone, chunk lazy séparé, 0 erreur console) ; smoke standalone OK (200/200/307 + headers). Écart documenté : `noUncheckedIndexedAccess` désactivé (150 erreurs sur code porté — dette légère post-portage). **Prochaine vague : V1** (schéma Prisma + auth + seed). |
| 2026-07-14 | **Dépôt GitHub créé et poussé** : `Worzee/livret-glm-app` (privé — via l'API avec le jeton git local, gh CLI absent). `main` suivie, 2 commits. |
| 2026-07-14 | **V1 LIVRÉE** (commits `19e61cb` + `3c3a05a`) : **schéma Prisma complet** (13 tables + 2 enums, doctrine « entité racine = table, agrégats = Json, dates ISO en String » documentée en tête de schéma), migration init, **seed = fixtures maquette** (19 utilisateurs, 8 livrets, 40 fiches, 24 documents — mdp démo commun `demo1234` bcrypt), **Auth.js v5 dual** (credentials + Entra conditionnel avec JIT/App Roles/écran attente), **droits côté serveur** (`requireSession/requireRole/requireDroit` sur la matrice), proxy Next 16, /login câblée, AppShell V1, tableau de bord branché (session → MariaDB → `apprentis-accessibles`). **Validation : 767 Vitest + 9 E2E smoke verts** (périmètres des 6 rôles exacts), build standalone vérifié en navigateur (login Martine → 5 apprenti·e·s). Piège découvert et capitalisé (STACK §8.3) : **`trustHost: true` obligatoire en prod** (UntrustedHost derrière proxy). MariaDB dev sur **3308** (3306/3307 pris par ASR/Matrice). **Prochaine vague : V2** (administration). |
| 2026-07-14 | **V2 EN COURS — module 1/10 livré** (commit `1fc99a2`) : **fondation admin** (design system PORTÉ en Tailwind 4 et vérifié par le DOM — risque du plan §7 LEVÉ ; Sidebar/MobileMenu portés avec nav par droits ; catch-all « module en portage » ; outillage reset E2E `/api/test/reset`) + **module Établissements/Pronote complet** (Server Actions gardées, page portée verbatim, spec 13 tests). **767 Vitest + 22 E2E verts.** Pièges : le proxy avalait le reset E2E (redirection suivie = faux succès → `/api/test` exclu + assertion JSON) ; 2 règles ESLint désactivées pour le code porté (apostrophes FR, setState-in-effect — dette documentée). **Pattern de portage posé** : chaque module suivant = actions + page client (chirurgie hooks→props) + page serveur + spec adaptée. Restent : utilisateurs, formations+planning, affectations, entreprises, référentiels+limite+paramètres, activités, attitudes, import XLSX. |
| 2026-07-14 | **V2 — modules 2-3/10 livrés** (commit `cdd2161`) : **Entreprises** et **Attitudes** portés par copie + chirurgie minimale sur le pattern établi (~30 min par module contre une demi-journée pour le premier). Nouveau chargeur `chargerLivrets` (agrégat livret + fiches — servira toute la V3). Specs portées : 7 tests actifs + 7 `fixme` documentés (chacun annoté du module qui le réactivera : Utilisateurs, Affectations, V3). **767 Vitest + 29 E2E verts.** Restent : utilisateurs (+ responsables légaux), formations (+ planning), affectations, référentiels (+ limite + paramètres), activités, import XLSX. |
| 2026-07-14 | **V2 — module 4/10 (Utilisateurs, le plus gros) LIVRÉ** (commit `574bf36`) : actions serveur avec toutes les cascades du store maquette (livret vierge + réalignements + sync maîtres + fratrie responsables légaux), page + 2 modales portées, 19 tests E2E du module (dont inscription d'une mineure avec contrôles d'emails). **767 Vitest + 48 E2E verts (10 fixme tracés).** 2 pièges capitalisés : (1) `entretien`/`cloture` sont `X \| null` — NULL SQL doit redevenir `null`, pas `undefined` (le verrou d'affectation teste `!== null`) ; (2) l'ordre des fixtures maquette n'est pas garanti par `findMany` → colonne `ordreInsertion` AUTO_INCREMENT (⚠ MySQL exige la clé dans la MÊME instruction — le diff Prisma les sépare, migration écrite à la main). Le SelecteurCoordoActif (artefact démo) est remplacé par la session. Restent : formations (+ planning), affectations, référentiels (+ limite + paramètres), activités, import XLSX. |
| 2026-07-14 | **V2 — module 5/10 (Formations + planning) LIVRÉ** (commit `810a0e9`) : la **cascade Formation → Livrets portée en SQL** (fiches vierges créées/mises à jour/supprimées dans tous les livrets de la promo, renumérotation), gardes du chantier #4 (mode d'évaluation, modèle, référentiel figé), réalignements des sélections de la promo. `BadgeMode` extrait en composant partagé. Bouton « Documents » de promo : compteur réel, dépôt en masse désactivé jusqu'à V4. **767 Vitest + 56 E2E verts** (7 tests formations dont la cascade planning sur les 6 livrets CAP). Restent : affectations, référentiels (+ limite + paramètres), activités, import XLSX. |
| 2026-07-14 | **V2 — module 6/10 (Affectations) LIVRÉ** (commit `7a10d06`) : cœur de mutation apprenti·e **factorisé** (`src/server/mutations-apprenti.ts`) et partagé entre saisie complète (modale) et patch champ par champ (auto-save des selects) ; convention « absent = ne pas toucher, null = effacer ». 9 tests portés (libération de Karim, répartition coordos par connexions réelles, double tutorat, persistance reload) + test entreprises RÉACTIVÉ. **767 Vitest + 66 E2E verts (9 fixme).** Restent : référentiels (+ limite + paramètres), activités, import XLSX. |
| 2026-07-14 | **✅ VAGUE V2 TERMINÉE — les 10 modules d'administration portés** (commit `7119706`) : référentiels (+ limite 40 lignes + paramètres), activités (+ mapping + mode d'évaluation) et import Excel livrés. Décision d'architecture : **le parsing CSV/XLSX reste côté client** (libs isomorphes, comme en maquette) — les actions reçoivent l'objet parsé et re-valident. **767 Vitest + 94 E2E verts (9 fixme V3/V4 tracés).** 3 corrections de fond : **checkboxes optimistes** (le store synchrone de la maquette masquait la latence — état local avec revert si refus serveur), **attente de fermeture de modale avant navigation** (naviguer annule le fetch d'une Server Action en vol), `__dirname` natif CJS. Catch-all /admin supprimé. **Prochaine vague : V3 — le livret pédagogique** (tableau de bord groupé, fiches de période, entretien tripartite, synthèse, clôture/R10 — la plus grosse du plan, ~3-4 j). |
| 2026-07-14 | **V3 EN COURS — module 1/5 (Tableau de bord) LIVRÉ** (commit `7607ee9`) : **l'« apprenti·e actif·ve » devient un cookie httpOnly** posé par Server Action et re-validé contre le périmètre à chaque requête (`resoudreApprentiActif`) ; contexte livret partagé (trio contextuel du header + lien entretien conditionnel câblés dans le layout) ; tableau de bord complet (groupes, badge pédagogie, pilotage KPI, centre d'alertes avec « Traité » à confirmation), récap apprenti·e R3, chargeurs documents SANS dataUrl. Les sélecteurs de démo disparaissent (1 rôle actif = 1 compte connecté) ; les specs V2 qui lisaient le placeholder V1 basculent sur les cartes réelles. Piège recapitalisé : attendre la redirection d'une Server Action avant un goto. **767 Vitest + 117 E2E verts (12 fixme tracés).** Restent : fiches de période, organisation du suivi, entretien, synthèse/clôture. |
| 2026-07-14 | **V3 — module 2/5 (Fiches de période) LIVRÉ** (commit `f239d74`) : co-édition tri-colonnes (modes compétences ET activités), attitudes sur fiche, observations par rôle, signatures tactiles (canvas porté tel quel), verrouillage R17 + déverrouillage R10 tracé. **R20/R21 appliquées côté serveur** (`validerSignature` + `peutEncoreEditerFiche` dans les actions, plus seulement dans l'UI) ; état de fiche recalculé par `deduireEtat` à chaque écriture. **Pattern nouveau : auto-save débouncé** (`TextareaAutoSave`, 600 ms + flush au blur, enregistrement silencieux) — remplace le « une frappe = une écriture store ». Séquencement gardé sur l'accès URL directe. 3 tests séquencement réactivés. **767 Vitest + 137 E2E verts (10 fixme).** Restent : organisation du suivi, entretien, synthèse/clôture. |
| 2026-07-14 | **V3 — module 3/5 (Organisation du suivi) LIVRÉ** (commit `2120427`) : événements par motif (filtrés par rôle côté serveur), verrou local + verrou « dur » R9 (entretien signé 3/3), suppression coordo/admin avec garde `peutSupprimerEvenement`, auto-save titre/date/commentaire (`InputAutoSave` créé). La cible des cartes du tableau de bord est complète. Adaptation E2E capitalisée : après un `fill` d'auto-save, flusher (blur) et laisser l'action aboutir avant `reload`. **767 Vitest + 150 E2E verts (11 fixme).** Restent : entretien tripartite, synthèse/clôture. |
| 2026-07-14 | **V3 — module 4/5 (Entretien tripartite) LIVRÉ** (commit `54a22fd`) : trame officielle + grille d'appréciation + 3 commentaires figés à la signature de leur auteur·rice + sélections §12 (compétences/activités/attitudes) + signatures R8/R9 (re-validées côté serveur) avec auto-marquage des sélections à la 3ᵉ signature, slot représentant légal (responsable ou formateur), aperçu lecture seule avant initialisation. **Pattern nouveau : cases optimistes à file séquentielle** (`useSelectionOptimiste` — deux toggles concurrents sur un agrégat JSON se perdraient). `entretienVierge()` extrait du store vers `lib/`. En-tête : maître/formateur résolus depuis les rattachements réels (écart maquette assumé — fixtures codées en dur). RÉACTIVÉS : 3 tests attitudes, init coordo, bouton Initialiser Sofia. Piège : ancrer l'URL après clic sidebar (« Entretien Tripartite » existe aussi sur les cartes d'organisation). **767 Vitest + 178 E2E verts (8 fixme).** Reste : synthèse / clôture R22-R10 + bout en bout. |
| 2026-07-14 | **✅ VAGUE V3 TERMINÉE — le livret pédagogique intégralement porté** (commit `5c79749`, 5 modules) : tableau de bord (+ pilotage/alertes/récap R3), fiches de période entreprise/centre (co-édition tri-colonnes, attitudes, signatures tactiles, R10/R17/R20/R21 serveur), organisation du suivi (R9), entretien tripartite (trame officielle, sélections §12 auto-marquées à la 3ᵉ signature, slot représentant légal), synthèse (héritage R23 / projection d'activités, écrasement confirmé) + clôture R22 avec réouverture. Redirection de parité `/livret/evaluation-finale`. **767 Vitest + 192 E2E verts — les 6 fixme restants appartiennent aux vagues V4 (3 : documents/responsables) et V5 (3 : exports PDF).** Le catch-all `/livret/[...reste]` ne sert plus que documents + accès mobile. **Prochaine vague : V4 — documents + Nuage** (~1-2 j). |
| 2026-07-14 | **✅ VAGUE V4 TERMINÉE — documents + Nuage.** (1) **Couche stockage `src/server/storage.ts`** : WebDAV Nuage en **fetch natif zéro dépendance** (doctrine §10.8, variante sans la lib `webdav` ESM-only ; MKCOL des collections parentes une à une — 405 = existe déjà), **repli disque local `.stockage-dev/`** quand `NEXTCLOUD_WEBDAV_URL` est vide (dev/E2E — repli déjà prévu par l'`env.example` du kit, jamais en prod) ; chemins par apprenti·e (nominatifs) et par promo (formation — lot B). (2) **Le binaire quitte la base** (doctrine §3.4) : migration `dataUrl` (LongText) → `cheminFichier` (référence), écrite à la main (le diff Prisma ajoutait du bruit `MODIFY … JSON`, artefact MariaDB) ; le type TS garde un `dataUrl` de parité maquette, toujours vide côté app ; le seed matérialise les PDF des fixtures au stockage (purge `documents/` pour l'idempotence du reset E2E). (3) **Actions serveur** (`actions/documents.ts`) : dépôt nominatif/promo en FormData (validation lib re-jouée + périmètre + remplacement par type avec purge de l'ancien binaire), consultation « lu et attesté » et attestation avec **attestataire re-validé CÔTÉ SERVEUR** (apprenti·e majeur·e / responsable légal rattaché d'un·e mineur·e — minorité recalculée), verrous de suppression (esprit R21). (4) **Routes GET gardées `/api/documents/{nominatif,formation}/[id]`** (session, périmètre, flag « réservé ») — réponses JSON 401/403, jamais de redirect (un `window.open` la suivrait) ; l'ouverture reste SYNCHRONE au clic (anti popup-blocker), le marquage « lu » part en parallèle puis `router.refresh()`. (5) Page `/livret/documents` + `ModaleDepotDocument` portées ; **dépôt en masse activé** sur la page Formations (liste de promo par props fraîches, pattern ModalePlanningPeriodes). E2E : spec documents portée (8 tests) + les **3 fixme responsables-legaux réactivés** (le sélecteur Thi↔Duc devient 2 connexions réelles ; l'import mineurs marchait déjà depuis V2, fixme jamais levé). **767 Vitest + 203 E2E verts (3 fixme restants → V5 exports PDF).** Warning de build « whole project traced » vérifié PRÉEXISTANT (requires dynamiques du client Prisma), pas introduit par la vague. **Prochaine vague : V5 — PDF + mobile** (~1-2 j). |
| 2026-07-15 | **✅ VAGUE V5 TERMINÉE — PDF + mobile : LA SUITE MAQUETTE EST INTÉGRALEMENT PORTÉE (767 Vitest + 225 E2E verts, 0 fixme).** (1) **Export PDF** : `LivretPdf.tsx` (1 372 l.) copié tel quel (wrapper `Text` anti-cadratins conservé ; imports d'images Vite → chemins `public/`), `ExportPdfLazy` + `BoutonExportPdf` portés en **`next/dynamic` `ssr: false`** (pattern V0 — chunk @react-pdf hors bundle initial), rôle en prop ; **chargeur serveur `donnees-pdf.ts`** remplace le hook `useDonneesLivretPdf` (maître/formateur résolus depuis les rattachements réels, fallbacks maquette ; documents effectifs V4 en métadonnées seules) ; boutons câblés sur Synthèse (livret complet), Fiches de suivi, Entretien, détail de période entreprise/centre. Piège TS : React 19 infère `ReactElement` nu en `unknown` → typer `ReactElement<DocumentProps>`. PDF **vérifié visuellement** (13 pages rasterisées PyMuPDF : garde + logos, documents administratifs V4 fusionnés, période mode activités, annexes R10). (2) **Accès mobile** : page QR portée (`qrcode-generator` ajouté ; origin lue au montage — `window` absent au 1ᵉʳ rendu SSR) ; catch-all `[...reste]` et smoke `dev-pdf` V0 SUPPRIMÉS (fin de mission). (3) **Audit mobile réintroduit** (projet Pixel 5, 12 tests) — il a attrapé **2 vrais défauts responsive** : `min-w-0` du `<main>` PERDU au portage V1 (le tableau de bord débordait à 880 px/375) et header non rétrécissable (identité `truncate` + libellés masqués `< sm`). Le test RoleSwitcher devient un audit du header de session. (4) **Doctrine E2E re-alignée sur la maquette : la suite teste le BUILD de production** (`next build && next start` dans le webServer — le mode dev devenait flaky à 225 tests ; suite 2× plus rapide : 4,7 min) + correctif du flaky `admin-affectations` (attendre la réponse de l'auto-save avant `reload` — piège V2/V3). ESLint : exemption `jsx-a11y/alt-text` sur `src/components/pdf/**` (primitives react-pdf, pas des `<img>` DOM). **Prochaine vague : V6 — comptes externes + emails** (chantiers 2.2/2.3, ré-arbitrage Mailjet vs SMTP académique au lot E), puis V7 (déploiement + recette). |
| 2026-07-15 | **Audit d'exhaustivité V0-V5** (routes, libs, composants, specs — vérification systématique contre la maquette) : parité confirmée, **2 écarts mineurs ACTÉS**. (1) **`reparation-livrets.spec.ts` volontairement NON portée** (30 specs portées sur les 31 du §5) : elle testait l'auto-réparation d'un livret effacé par un bump de version localStorage — mécanisme qui n'existe plus dans la stack cible (§1.D : le reset par version disparaît ; l'intégrité est transactionnelle, la création d'un·e apprenti·e crée son livret dans la même Server Action — cascades V2). Obsolète par conception, aucune couverture perdue. (2) **Pas de page 404 personnalisée** (la maquette avait `NotFound.tsx`) : Next sert son 404 par défaut — cosmétique, à reprendre au polish de la **V7** (`app/not-found.tsx`). Aucune fonctionnalité utilisateur manquante par ailleurs ; risque §7 « uploads Passenger » toujours ouvert pour V7, driver WebDAV Nuage à tester au lot B. |
| 2026-07-15 | **✅ VAGUE V6 TERMINÉE — comptes externes + emails (chantiers 2.2/2.3)** (commit `b98ba61`) : **812 Vitest + 234 E2E verts (222 desktop + 12 mobile, 0 fixme)**. (1) **Libs pures TDD** (45 tests) : `jetons-compte` (32 octets, SHA-256 seul en base, usage unique, 7 j / 1 h — décisions 2/9), `mot-de-passe` (option A : 12 caractères min), `rate-limit` (fenêtre glissante + les 4 règles §8.5), `statut-compte` (actif/en-attente/sso dérivé, jamais stocké), `emails-compte` (templates §7 texte+HTML purs, échappement, dates fr). (2) **Schéma** : colonnes `activeLe` + `mentionsAcceptees{Version,Le}` (RGPD §8.2), tables `jetons_compte` (FK cascade), `evenements_audit` (LIENS SANS FK — un log survit à la suppression du compte), `tentatives_connexion` ; migration écrite à la main (bruit `MODIFY … JSON` écarté, cf. V4). (3) **Transport email AGNOSTIQUE** (`server/email.ts`, pattern storage V4) : SMTP nodemailer si `SMTP_HOST`+`SMTP_USER` posés, sinon **repli fichier `.emails-dev/`** lu par `/api/test/emails` (E2E lisent les liens comme une boîte mail) — **le ré-arbitrage Mailjet vs relais académique (lot E) devient une pure configuration**, les deux étant du SMTP. (4) **Parcours** : `/activer/[jeton]` (4 états, mentions OBLIGATOIRES tracées `v1.0-2026-07`, connexion AUTOMATIQUE post-activation), `/reinitialiser/[jeton]`, `/mot-de-passe-oublie`, `/mentions-legales` (⚠ à VALIDER DPO avant données réelles), page `/compte` (changement mdp 2.3), badge « En attente d'activation » + bouton renvoi admin (nouvelle ressource `envoyer-activation` : coordo+admin), case d'envoi cochée par défaut (apprenti, maître, responsables légaux nouvellement créés — demande 5 ; l'import XLSX N'ENVOIE PAS : envoi en masse = décision explicite). (5) **Sécurité** : bcrypt coût 12 (décision 10, repli explicitement accepté — argon2 natif = risque d'installation CloudLinux), anti-énumération TOTALE (les refus publics, rate limit inclus, sont silencieux — plus étanche que la spec qui prévoyait des 429), rate limit connexion 5/15 min en base + anti-scan jetons 10/min/IP en mémoire process, journal d'audit francisé (`compte.cree`, `connexion.echouee`…), un compte jamais activé qui demande « mot de passe oublié » reçoit un lien d'ACTIVATION. (6) **Cron** `/api/cron/quotidien` (Bearer `CRON_SECRET`, pattern kit) : notification coordo J+7 (§7.3) + purges RGPD (jetons 30 j, tentatives 24 h, audit 12 mois) — **à câbler dans cPanel en V7**. Pièges capitalisés : reset E2E étendu (purge audit/tentatives/emails/limites mémoire — sinon le test de rate limit bloque les specs suivantes) ; le Button base-ui n'a pas `asChild` (→ `buttonVariants` sur `<Link>`) ; flaky `limite-referentiel` corrigé (attendre le POST du seuil avant d'ouvrir la modale d'import — les requêtes login V6 avaient déplacé le timing). **Décision 7 re-cadrée PAR DÉFAUT (parité maquette, à confirmer pilote) : le compte d'un·e mineur·e s'active dès l'inscription**, ses responsables légaux ayant leurs propres comptes. **Prochaine vague : V7 — déploiement + recette** (runbook kit, not-found, upload 2 Mo Passenger, cron cPanel). |
| 2026-07-15 | **Retouche V6 (décision pilote) : le lien d'activation passe de 7 à 30 jours** (commit `42952a1`) — décision 2 du chantier création comptes AMENDÉE. Durée centralisée dans `DUREES_JETON_MS`, textes alignés (email, page /activer, modales), la notification coordo « lien expiré » se déclenche désormais à J+30. La réinitialisation reste à 1 heure. **812 Vitest + 234 E2E verts.** |
| 2026-07-18 | **V7 AMORCÉE — polish déploiement (1er lot, sans prérequis externe)** (commit `28d6f11`) : **813 Vitest + 236 E2E verts (224 desktop + 12 mobile)**. (1) **404 personnalisée** `app/not-found.tsx` (écart acté à l'audit V0-V5) : rendue par le root layout hors AppShell/session — visible pour une route inexistante d'un compte connecté et sous les préfixes publics ; cohérente écrans publics V6 ; lien vers `/` (la garde oriente login/tableau). Spec `not-found.spec.ts` (public → login, connecté → tableau). (2) **Préflight o2switch** `scripts/preflight-o2switch.sh` — successeur de `verifier-vps.sh` de la maquette (VPS Docker/Traefik/Basic Auth), **réécrit pour la cible Passenger + AUTH APPLICATIVE réelle + 404 réelles** : 16 contrôles (login 200, racine→/login, pages publiques, `/api/test/*` fermé en 403, `/api/cron` sans Bearer→401, vrai 404, headers sécu, anti-télémétrie ; SSL/HSTS/redirection 80→443 auto-ignorés en http). Tourne contre la prod OU `localhost:3000` — **vérifié 16 OK / 0 KO** contre le build local. (3) **Upload 2 Mo (risque §7 SOLDÉ côté appli)** : la validation métier 2 Mo est appliquée CÔTÉ CLIENT (bloque l'envoi) ET serveur → un fichier trop gros n'atteint jamais Passenger, sous le `bodySizeLimit` 4 Mo ; message d'erreur dépoussiéré (résidu « maquette localStorage / étape 2 » retiré), test unitaire renforcé. ⚠ **Reste à vérifier en recette (lot D)** : la limite de POST de LiteSpeed sur o2switch (doit être ≥ 4 Mo). **Restent pour clore V7** : runbook de bascule (kit §0-9), câblage cron cPanel de `/api/cron/quotidien`, recette pilote, bascule DNS — + prérequis admin lots A-E (DPO, Nuage, Entra, domaine, SMTP). |
| 2026-07-18 | **V7 — LE RESTE TECHNIQUE EST LIVRÉ : runbook de bascule + cron cPanel documentés** (commit `1f96a1a`) : **`docs/RUNBOOK_BASCULE.md`** — séquence de mise en production SPÉCIFIQUE au projet (le générique reste dans `DEPLOIEMENT_O2SWITCH.md`) : prérequis P1-P6 adossés aux lots A-E (**Entra OPTIONNEL à la bascule** — provider conditionnel, credentials en attendant le lot C), garde-fou DPO en tête, **`.env` de prod COMPLET** (Auth/Entra/SMTP/CRON_SECRET/Nuage — un `.env` incomplet = pannes silencieuses), migrations phpMyAdmin (4 fichiers concaténés, 16 tables attendues), **seed des fixtures via TUNNEL SSH** (le seed écrit la base ET matérialise les PDF sur le Nuage de prod — il tourne depuis le poste avec le `.env` de prod ; repli Remote MySQL), préflight 19/19 attendu, **cron cPanel quotidien 05h17** (CRON_SECRET lu du `.env` par `sed`, rien en clair dans la crontab, test manuel documenté), recette pilote en 13 points (dont vérif limite POST LiteSpeed ≥ 4 Mo, et interdiction de jouer la suite E2E contre la prod — elle reseederait la base), bascule + décommission du VPS duckdns (rollback naturel pendant la recette). Divers : script `pnpm e2e` (parité maquette), AGENTS.md pointe runbook + préflight. **La V7 n'attend plus QUE les prérequis administratifs (lots A-E) puis l'exécution du runbook** — plus aucun développement au plan de portage. |
| 2026-07-18 | **🚀 BASCULE EFFECTUÉE — `https://livret.gretacfalyon.com` EN LIGNE (FIXTURES), préflight 19 OK / 0 KO.** Runbook §1→§11 déroulé en séance pilote : pré-bascule locale re-validée (813 Vitest + 236 E2E verts), bundle standalone 21 Mo (SANS `node_modules` ni `.env*` — exclusions ajoutées au runbook, `package.json` complet substitué AVANT archivage), sous-domaine + Let's Encrypt + base + Node.js App 22 (`start.cjs`) + `.env` prod 18 variables (Entra vide — bouton SSO désactivé avec mention, comme conçu) + `npm install` serveur (417 paquets), migrations phpMyAdmin (**15 tables** — la coquille « 16 » du runbook est corrigée : 12 V1 + 3 V6), **seed 19 utilisateurs / 8 livrets / 40 fiches / 24 documents avec PDF matérialisés sur le Nuage réel**. ⚠ Découverte poste : port 22 sortant FILTRÉ → le seed est passé par le repli Remote MySQL® du runbook (host `pif.o2switch.net:3306`, IP ouverte puis refermée aussitôt). 2 accrocs résolus et capitalisés au runbook §10 : (1) Passenger non raccordé au sous-domaine au 1ᵉʳ essai (pages statiques o2switch → re-Save de l'Application URL + Restart) ; (2) redirection 80→443 non automatique (RewriteRule posée EN TÊTE du `.htaccess` du Document Root, bloc Passenger intact). **Cron cPanel 05h17 câblé** : chaîne complète validée par test manuel (`{"ok":true,"notifies":0,"purges":{...}}` dans `~/logs/cron-livret.log` ; 401 sans Bearer) — contrôler le log à J+1. **Recette §12 ENTAMÉE en ligne** : connexion coordo Martine (5 apprenti·e·s, KPI et alertes R7 conformes), parcours livret Théo (3 périodes signées, synthèse par blocs en lecture seule coordo), **lecture Nuage en PROD validée** (`GET /api/documents/nominatif/...` → 200 `application/pdf` 958 o). Restent en recette MANUELLE : dépôt Nuage + refus > 2 Mo (limite POST LiteSpeed ≥ 4 Mo), export PDF 13 pages, activation SMTP réelle, mot de passe oublié, renvoi de lien admin, parcours mobile réel. Le VPS duckdns reste le rollback jusqu'à la fin de recette (§13 : comm + décommission ensuite). Données réelles toujours conditionnées à la validation DPO (lot A). |
| 2026-07-19 | **🔴 BLOQUANT PROD DIAGNOSTIQUÉ — la couche cache o2switch mélange les réponses entre vhosts (ticket support en cours).** Symptôme (recette §12) : les Server Actions « ne font rien » en prod (clic carte/connexion hydratée sans navigation) alors que préflight 19/19 et flux « sans JS » sains. Enquête méthodique (rejeux curl avec session, A/B local vs `next start` vs standalone vs 127.0.0.1 SUR le serveur, cache-busters, injection volontaire de headers de prefetch, autopsie du corps) : la réponse 303 traverse la couche `o2switch-PowerBoost-v3` avec ses **headers vivants** (`x-action-redirect` suit la destination, `Set-Cookie` applicatif posé) mais un **corps substitué de 5 741 octets — le payload RSC de l'app ASR** (`"title":"ASR — Greta CFA Lyon Métropole"`), + `x-nextjs-cache: HIT`/`stale-time`/`prerender: 1, 1` (valeur dédoublée = fusion type « revalidation » RFC 9111 appliquée à tort à notre 303, comme un 304). Pistes ÉLIMINÉES une à une : app (saine en direct sous tous headers, 36-42 Ko par destination), cache navigateur (repro curl), redirection http (Location relative), rate limit V6, injection de headers de prefetch (sans effet en direct), **notre propre config** (audit : AppRoot/venv Passenger distincts par app, pas d'AppGroupName, `next.config.ts` sans cacheHandler, AUCUN `.next/cache` disque, `grep` ASR négatif dans l'arborescence livret, corps figé survivant aux Restart + veilles Passenger → l'entrée vit hors de notre périmètre). Leviers utilisateur épuisés : `no-store` émis (ignoré), `Vary` applicatif (supprimé), `CacheDisable`, `E=Cache-Control:no-cache`, XtremCache inactif, LSCache WebADC non activé, cache-buster d'URL (« HIT » même sur URL inédite : clé insensible à la query). **GET propres et stables** (mélange confiné aux POST d'actions). ⚠ Signalé comme faille d'étanchéité inter-sites dans le ticket (contenu d'un site servi sur un autre). Runbook §10 annoté. **La recette §12 est SUSPENDUE sur la navigation par clics** (lecture + flux document OK) en attente o2switch ; plan B applicatif identifié si besoin (actions sans `redirect()` + `router.push` client). |
| 2026-07-19 | **Contre-enquête finale « rien au hasard » (demande pilote) — corruption LOCALISÉE sur la boîte o2switch.** Pistes supplémentaires éliminées avec preuves : réseau/MITM du poste (certificat Let's Encrypt authentique bout en bout, aucun proxy Windows), protocole (cassé en HTTP/1.1 ET HTTP/2), compression (idem ±), datation de l'entrée figée (chunks = build ASR ACTUEL). Test décisif : **le rejeu depuis le serveur lui-même via son propre front local** (`curl --resolve livret…:443:127.0.0.1` en SSH sur pif) reproduit la corruption à l'identique (5 741 o, `x-nextjs-cache: HIT`, `prerender: 1, 1`) → il ne reste que deux composants possibles, tous deux o2switch : LiteSpeed/PowerBoost local ou module Passenger (version non lisible en mutualisé, à demander au support). Annexe ajoutée au ticket. Bilan : 10 familles de pistes examinées, 9 éliminées avec preuve, 1 désignée. |
| 2026-07-19 | **Plan B validé par mesure directe : les actions à réponse 200 traversent la couche o2switch INTACTES.** Test en prod (session maître Karim sur la fiche P3 de Léa — seul écrivable sans risque : 1/2 signatures, côté maître libre R20/R21) : deux auto-saves capturés (ajout d'une espace puis restauration de la valeur d'origine — fixture finale inchangée) → **200, préambule d'action canonique `0:{"a":…}`, 81 o, AUCUN header de cache parasite, aucun contenu ASR**. Cartographie finale de la corruption : GET sains, POST `/api/*` sains, actions-500 saines, actions-200 saines, **seuls les 303 d'actions sont corrompus** (fusion type 304 limitée aux 3xx). → Si le ticket o2switch revient négatif, le contournement retenu (décision pilote : « niveau 1 ») est SÛR : remplacer les `redirect()` des ~6-8 actions concernées (activerApprenti, connexion/déconnexion, activation/réinitialisation) par un retour `{ ok, destination }` + `router.push()` client — ~½ journée + re-validation E2E, réversible, et durcissement pérenne contre tout cache indélicat. |
| 2026-07-19 | **✅ PLAN B LIVRÉ ET DÉPLOYÉ — la navigation cliquée FONCTIONNE en prod malgré le bug o2switch (décision pilote « niveau 1 »).** Conversion de TOUTES les Server Actions à `redirect()` vers le pattern « retour `{ destination }` + navigation client `window.location.assign` » : `activerApprenti` (cartes + alertes du tableau de bord), `connexionClassique`/`deconnexion` (nouveaux composants client `formulaire-connexion.tsx` — erreurs rendues par état, plus de `?erreur=` — et `bouton-deconnexion.tsx`), `activerCompteAction`/`reinitialiserAction` (contrat du `FormulaireDefinitionMdp` étendu à `{ destination }`). `connexionGreta` (SSO Entra) conservée telle quelle : cible EXTERNE suivie par header, non concernée. Les `redirect()` des pages/layouts/proxy (GET sains) inchangés. **Piège React 19 découvert par la suite E2E** (seul échec : spec rate limiting) : React réinitialise le formulaire après chaque action → une saisie faite pendant l'envoi précédent était effacée et le `required` bloquait la soumission suivante → champs de connexion passés en mode CONTRÔLÉ + désactivés pendant l'envoi (au passage : l'email saisi survit désormais à un échec). **813 Vitest + 236 E2E verts**, bundle redéployé (procédure §14 ; nouveau piège capitalisé : exclure l'archive précédente du tar — NFT la trace sinon, 42 Mo au lieu de 21). **Preuves en prod** : action → 200/70 Ko sans `x-action-redirect` (plus rien à corrompre), préflight 19/19, login hydraté ET clic de carte naviguent (page Fiches de suivi de Théo atteinte). RÈGLE inscrite à l'AGENTS.md + runbook §10 : jamais de `redirect()` dans une action tant que le ticket o2switch (mélange inter-vhosts) n'est pas clos. La recette §12 est DÉBLOQUÉE ; prochaine session : modifications mineures de fonctionnalités. |
| 2026-07-20 | **✅ SESSION POLISH UI/TEXTES — 4 lots livrés et DÉPLOYÉS (procédure §14).** (1) Plus aucun code de règle visible : « Alertes R7 » → « Alertes », badge « N alerte(s) » sans code, bandeau ambre et message du centre d'alertes sans « (R7) », description de l'événement Entretien Tripartite et fixtures nettoyées (⚠ base prod seedée : les 3 commentaires « (R7) » déjà en base restent — pas de reseed pour préserver la recette pilote). (2) Sous-titres explicatifs supprimés sous les titres de 8 pages (Tableau de bord, Documents administratifs, Fiches de suivi, Entretien tripartite ×2 variantes, Période en Entreprise/Centre, Synthèse, Pronote WEB) — la mention « mineur·e → attestation responsables légaux » du sous-titre Documents part avec (le bandeau d'attestation porte toujours la logique), 2 assertions E2E adaptées. (3) Plus aucun cadratin « — » visible (24 chaînes réellement affichées sur 1 184 occurrences, le reste = commentaires/noms de tests) : remplacements contextuels (point, virgule, deux-points, parenthèses, point médian, séparateur « -- » des signatures email) ; wrapper anti-cadratins du PDF conservé en filet. (4) Rappel « Apprenti·e : Prénom NOM » ajouté sous les titres Tableau de bord (apprenti·e actif·ve résolu·e côté serveur via le cookie revalidé) et Documents administratifs ; en-tête mobile auto-révélé (sticky < md : masqué à la descente, réapparaît dès ~8 px de remontée — comportement validé pilote). **813 Vitest + 236 E2E verts** (suite complète re-jouée avant envoi, en 3 tranches sur serveur `next start` réutilisé), bundle 21 Mo / 1 133 fichiers (contrôles §1 : 0 fichier interdit), remplacement `.next` + Restart via cPanel (pilote), **préflight 19/19**, nouveaux textes vérifiés en prod (title, mentions légales). Commits app : `d1ed25a`, `bba7994`, `5180007`, `a743fa2`. |
| 2026-07-20 | **✅ 2ᵉ DÉPLOIEMENT DU JOUR — encart de signature épuré + zéro code de règle visible.** (1) L'encart de confirmation de signature ne montre plus que « Vous allez signer en tant que <rôle : Prénom NOM>. » (consigne de tracé et mention R10 supprimées, prop `mentionRetrait` morte retirée). (2) Passe exhaustive sur les 22 mentions restantes de codes de règles (R2-R22) : notes d'interface, badge « traçabilité », titre de section de l'export PDF, messages de validation des formulaires (import Excel, planning, suppression de fiche) et gardes serveur. Scanner : 0 code restant dans les chaînes visibles ; les références réglementaires restent dans le CDC, les commentaires et les noms de tests. 2 assertions alignées sur le contenu des messages (Vitest R11 + spec E2E fiches-periodes, seul échec du run complet, corrigé et rejoué vert). **813 Vitest + 236 E2E verts**, archive 21 Mo / 1 133 fichiers, remplacement `.next` + Restart cPanel (pilote), **préflight 19/19**, bundle vérifié en ligne (chunks servis = build local). Commits app : `2726136`, `0d6ae47`, `8760275`. |
| 2026-07-20 | **✅ ENTRETIENS ANNUELS développés (5 vagues, ~2 400 lignes) — EN ATTENTE DE DÉPLOIEMENT.** Nouvelle fonctionnalité complète, calquée sur l'entretien tripartite mais FACULTATIVE : (V1) 2 tables (`questions_entretien_annuel` catalogue admin ; `entretiens_annuels` 0-3 par livret, liés à l'événement générateur — doctrine FicheSuivi, signatures PNG en table séparée), types, lib TDD (limite 3, verrouillage miroir R8/R9, choix de questions figé dès la 1ʳᵉ signature, zones de réponse par répondant — le formateur co-saisit côté maître, jamais la zone apprenti), 12 ressources de droits (catalogue admin SEUL). (V2) Page `/admin/questions-annuelles` (modèle Attitudes, badge répondant) + **20 questions initiales** seedées (bilan, difficultés/accompagnement, projet pro, objectifs). (V3) Motif « Entretien annuel » des Fiches de suivi (formateur/coordo/admin, limite 3 UI+serveur, carte figée/insupprimable dès signature), liens sidebar dynamiques, page complète `/livret/entretien-annuel/[evt]` (initialisation, sélection de questions copiées — les orphelines du catalogue survivent —, réponses par zone, expressions libres des 3 parties, 3 signatures + slot représentant légal). (V4) Onglet « Entretiens annuels » de la Synthèse (contenu extrait) + PDF (section du livret complet + export partiel « Exporter cet entretien »). (V5) Spec E2E de 8 tests bout en bout + SQL de déploiement phpMyAdmin (`prisma/deploiement/2026-07-20-entretiens-annuels.sql`, rejouable, base pilote préservée). **Suite verte : 844 Vitest (+31) + 244 E2E (+8)**, chaque vague vérifiée en local au navigateur (formatrice, apprentie, admin, PDF généré). Commits app `de38d51`, `5838c10`, `cce0a26`, `3552e44`, `66e2405`, `3a9649c`. **Déploiement : SQL phpMyAdmin PUIS bundle §14** — à programmer avec le pilote. |
| 2026-07-21 | **✅ ENTRETIENS ANNUELS DÉPLOYÉS EN PROD.** Première mise à jour avec migration depuis la bascule : SQL via phpMyAdmin (2 tables + FK + 20 questions, script `prisma/deploiement/2026-07-20-entretiens-annuels.sql`) PUIS bundle §14 (22 Mo / 1 186 fichiers). Deux pièges capitalisés : (1) le `IF NOT EXISTS` d'une contrainte FK se place APRÈS « FOREIGN KEY » en MariaDB (erreur #1064, corrigé `0a84b5c`) ; (2) ⚠ phpMyAdmin avait la base **`tlxn8907_asr`** sélectionnée — le premier import y a créé nos 2 tables (nettoyées par DROP ciblés), TOUJOURS vérifier le bandeau « base de données » avant d'exécuter. Préflight **19/19**, vérification applicative en prod : login admin OK (plan B), `/admin/questions-annuelles` affiche les 20 questions (preuve nouveau bundle + nouvelles tables). La recette pilote peut couvrir les entretiens annuels. |
